<!DOCTYPE html>
<html>
  <head>
    <title>BudgetApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "category", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "transaction", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "forms", "data-turbo-track": "reload" %>
    
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,300;1,400;1,500;1,700&display=swap" rel="stylesheet">
  </head>

  <body>
  <div class="application-header">
  <div class="application-main-info">
  <%= image_tag "budget-app.png", alt: "Buget App" %>
  </div>

<div class="application-user-info">
      <% if user_signed_in? %>
      <div class="user-edit-password">
        <div class="user-image-login"><p>Welcome <span><%= current_user.name %></span></p><img src=<%= current_user.photo %> alt=<%= current_user.name %> class= "user-image" > </div>
      </div>
      <div>
      <%= button_to "Logout", destroy_user_session_path, method: :delete, data: { turbo: false }, class: "nav-login" %>
      </div>
        <% else %>
        <div><%= link_to "Log in", new_user_session_path, class: "nav-login" %> </div>
      <% end %>
    </div>
    </div>

    <div class="notification-area">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </div>
    <%= yield %>
  </body>
</html>
